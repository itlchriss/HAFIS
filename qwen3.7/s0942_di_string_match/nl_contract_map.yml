problem: s0942_di_string_match
method: diStringMatch
total_annotations: 12
requires_count: 3
ensures_count: 9
original_contract_count: 6
annotations:
- id: 0
  type: requires
  nl: The length of the string parameter `s` is greater than or equal to 1 and is
    less than or equal to 100000.
  original_contract: 1 <= |s| <= 100000
- id: 1
  type: requires
  nl: The string parameter `s` consists of only the characters 'I' or 'D'.
  original_contract: 'forall i: nat :: i < |s| ==> s[i] == ''I'' || s[i] == ''D'''
- id: 2
  type: requires
  nl: The string parameter `s` is not equal to the null literal.
  original_contract: result != null
- id: 3
  type: ensures
  nl: The integer array result is not equal to the null literal.
  original_contract: result.Length == |s| + 1
- id: 4
  type: ensures
  nl: The length of the integer array result is equal to the length of the string
    parameter `s` plus 1.
  original_contract: 'forall i: nat :: i < result.Length ==> 0 <= result[i] <= |s|'
- id: 5
  type: ensures
  nl: All values in the integer array result are greater than or equal to 0 and are
    less than or equal to the length of the string parameter `s`.
  original_contract: 'forall i: nat :: i < |s| ==>'
- id: 6
  type: ensures
  nl: All values in the integer array result are unique.
- id: 7
  type: ensures
  nl: For every non-negative integer `the string parameter `s`` array result is less
    than the length of the string parameter `s`, if the character at index `the string
    parameter `s`` of the string parameter `s` is equal to 'I', the value at index
    `the string parameter `s`` of the integer array result is less than the value
    at index `the string parameter `s`` plus 1 of the integer array result.
- id: 8
  type: ensures
  nl: For every non-negative integer `the string parameter `s`` array result is less
    than the length of the string parameter `s`, if the character at index `the string
    parameter `s`` of the string parameter `s` is equal to 'D', the value at index
    `the string parameter `s`` of the integer array result is greater than the value
    at index `the string parameter `s`` plus 1 of the integer array result.
- id: 9
  type: ensures
  nl: If the string parameter `s` is equal to "IDID", the integer array result is
    equal to [0,4,1,3,2].
- id: 10
  type: ensures
  nl: If the string parameter `s` is equal to "III", the integer array result is equal
    to [0,1,2,3].
- id: 11
  type: ensures
  nl: If the string parameter `s` is equal to "DDI", the integer array result is equal
    to [3,2,0,1].
