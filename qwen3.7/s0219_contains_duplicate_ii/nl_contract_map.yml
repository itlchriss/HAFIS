problem: s0219_contains_duplicate_ii
method: containsNearbyDuplicate
total_annotations: 9
requires_count: 4
ensures_count: 5
original_contract_count: 4
annotations:
- id: 0
  type: requires
  nl: The length of the integer array parameter `nums` is greater than or equal to
    1 and is less than or equal to 100000.
  original_contract: 1 <= nums.Length <= 100000
- id: 1
  type: requires
  nl: All values in the integer array parameter `nums` are greater than or equal to
    -1000000000 and are less than or equal to 1000000000.
  original_contract: 'forall i: nat :: i < nums.Length ==> -1000000000 <= nums[i]
    <= 1000000000'
- id: 2
  type: requires
  nl: The integer parameter `k` is greater than or equal to 0 and is less than or
    equal to 100000.
  original_contract: 0 <= k <= 100000
- id: 3
  type: requires
  nl: The integer array parameter `nums` is not equal to the null literal.
  original_contract: 'result <==> (exists i: nat, j: nat ::'
- id: 4
  type: ensures
  nl: If the boolean result is equal to the true literal, there exist distinct non-negative
    integers `the int array parameter `nums`` and `j` such the boolean result `the
    int array parameter `nums`` is less than the length of the integer array parameter
    `nums` and `j` is less than the length of the integer array parameter `nums` and
    the absolute difference between `the int array parameter `nums`` and `j` is less
    than or equal to the integer parameter `k` and the value at index `the int array
    parameter `nums`` of the integer array parameter `nums` is equal to the value
    at index `j` of the integer array parameter `nums`.
- id: 5
  type: ensures
  nl: If the boolean result is equal to the false literal, there do not exist distinct
    non-negative integers `the int array parameter `nums`` and `j` such the boolean
    result `the int array parameter `nums`` is less than the length of the integer
    array parameter `nums` and `j` is less than the length of the integer array parameter
    `nums` and the absolute difference between `the int array parameter `nums`` and
    `j` is less than or equal to the integer parameter `k` and the value at index
    `the int array parameter `nums`` of the integer array parameter `nums` is equal
    to the value at index `j` of the integer array parameter `nums`.
- id: 6
  type: ensures
  nl: If the integer array parameter `nums` is equal to [1,2,3,1] and the integer
    parameter `k` is equal to 3, the boolean result is equal to the true literal.
- id: 7
  type: ensures
  nl: If the integer array parameter `nums` is equal to [1,0,1,1] and the integer
    parameter `k` is equal to 1, the boolean result is equal to the true literal.
- id: 8
  type: ensures
  nl: If the integer array parameter `nums` is equal to [1,2,3,1,2,3] and the integer
    parameter `k` is equal to 2, the boolean result is equal to the false literal.
