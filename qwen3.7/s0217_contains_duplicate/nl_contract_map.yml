problem: s0217_contains_duplicate
method: containsDuplicate
total_annotations: 8
requires_count: 3
ensures_count: 5
original_contract_count: 2
annotations:
- id: 0
  type: requires
  nl: The length of the integer array parameter `nums` is greater than or equal to
    1 and is less than or equal to 100000.
  original_contract: 1 <= nums.Length <= 100000
- id: 1
  type: requires
  nl: All values in the integer array parameter `nums` are greater than or equal to
    -1000000000 and are less than or equal to 1000000000.
  original_contract: 'result <==> (exists i: nat, j: nat :: i < j < nums.Length &&
    nums[i] == nums[j])'
- id: 2
  type: requires
  nl: The integer array parameter `nums` is not equal to the null literal.
- id: 3
  type: ensures
  nl: If the boolean result is equal to the true literal, there exist a non-negative
    integer `the int array parameter `nums`` and a non-negative integer `j` such the
    boolean result `the int array parameter `nums`` is less than `j` and `j` is less
    than the length of the integer array parameter `nums` and the value at index `the
    int array parameter `nums`` of the integer array parameter `nums` is equal to
    the value at index `j` of the integer array parameter `nums`.
- id: 4
  type: ensures
  nl: If the boolean result is equal to the false literal, all values in the integer
    array parameter `nums` are unique.
- id: 5
  type: ensures
  nl: If the integer array parameter `nums` is equal to [1,2,3,1], the boolean result
    is equal to the true literal.
- id: 6
  type: ensures
  nl: If the integer array parameter `nums` is equal to [1,2,3,4], the boolean result
    is equal to the false literal.
- id: 7
  type: ensures
  nl: If the integer array parameter `nums` is equal to [1,1,1,3,3,4,3,2,4,2], the
    boolean result is equal to the true literal.
