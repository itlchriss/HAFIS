problem: s0231_power_of_two
method: isPowerOfTwo
total_annotations: 9
requires_count: 1
ensures_count: 8
original_contract_count: 2
annotations:
- id: 0
  type: requires
  nl: The integer parameter `n` is greater than or equal to -2147483648 and is less
    than or equal to 2147483647.
  original_contract: 'n > 0 ==> (result <==> (exists x: nat :: n == pow2(x)))'
- id: 1
  type: ensures
  nl: If the integer parameter `n` is greater than 0 and the boolean result is equal
    to the true literal, there exists a non-negative integer `x` such the boolean
    result the integer parameter `n` is equal to 2 raised to the power of the non-negative
    integer `x`.
  original_contract: n <= 0 ==> !result
- id: 2
  type: ensures
  nl: If the integer parameter `n` is greater than 0 and the boolean result is equal
    to the false literal, there does not exist a non-negative integer `x` such the
    boolean result the integer parameter `n` is equal to 2 raised to the power of
    the non-negative integer `x`.
- id: 3
  type: ensures
  nl: If the integer parameter `n` is less than or equal to 0, the boolean result
    is equal to the false literal.
- id: 4
  type: ensures
  nl: If the integer parameter `n` is equal to 1, the boolean result is equal to the
    true literal.
- id: 5
  type: ensures
  nl: If the integer parameter `n` is equal to 16, the boolean result is equal to
    the true literal.
- id: 6
  type: ensures
  nl: If the integer parameter `n` is equal to 3, the boolean result is equal to the
    false literal.
- id: 7
  type: ensures
  nl: If the integer parameter `n` is equal to 4, the boolean result is equal to the
    true literal.
- id: 8
  type: ensures
  nl: If the integer parameter `n` is equal to 5, the boolean result is equal to the
    false literal.
