- The length of the integer array parameter `cost` is equal to the length of the integer array parameter `gas`.
- All values in the integer array parameter `gas` are greater than or equal to 0 and are less than or equal to 10000.
- All values in the integer array parameter `cost` are greater than or equal to 0 and are less than or equal to 10000.
- The integer result is greater than or equal to -1 and is less than the length of the integer array parameter `gas`.
- If the integer result is equal to -1, the sum of all values in the integer array parameter `gas` is less than the sum of all values in the integer array parameter `cost`.
- If the integer result is greater than or equal to 0, starting from the index equal to the integer result and moving clockwise, the running total of the difference between each value in the integer array parameter `gas` and the corresponding value in the integer array parameter `cost` is never negative and the traversal returns to the starting index.
- If the integer result is greater than or equal to 0, no other index in the integer array parameter `gas` satisfies the previous specification.
- If the integer array parameter `gas` is equal to [1,2,3,4,5] and the integer array parameter `cost` is equal to [3,4,5,1,2], the integer result is equal to 3.
- If the integer array parameter `gas` is equal to [2,3,4] and the integer array parameter `cost` is equal to [3,4,3], the integer result is equal to -1.
