ensures:
- boolean result is equal to the true literal if int[] the type_string_ strs_a contains at least 3 elements and there exists an index strictly between the first and last positions that the values strictly increase up to that index and strictly decrease afterwards.
- boolean result is equal to the false literal when int[] the type_string_ strs_a does not satisfy the mountain array condition described above.
requires: []
