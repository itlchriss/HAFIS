requires((\exists int i; 0 <= i < str.length(); Character.isLetter(str.charAt(i))) && ((\exists int i; 0 <= i < str.length(); Character.isDigit(str.charAt(i))) && ((\exists int i; 0 <= i < str.length(); (str.charAt(i) == ' ')) && ((\exists int i; 0 <= i < str.length(); (str.charAt(i) == '+')) && ((\exists int i; 0 <= i < str.length(); (str.charAt(i) == '-')) && (\exists int i; 0 <= i < str.length(); (str.charAt(i) == '.')))))));
